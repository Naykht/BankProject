using banks.Model;
using System;

namespace banks
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
            var n = DateTime.Now;
            Console.WriteLine(n.ToString("dd/MM/yyyy"));
        }
    }
}
