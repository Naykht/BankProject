<Window x:Class="BankManager.MakeTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BankManager"
        mc:Ignorable="d"
        Title="AddClientWindow" Height="290" Width="377">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="187"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">From:</TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center"   VerticalAlignment="Center">To:</TextBlock>
        <ComboBox Name="accSenderCombo" Grid.Row="1" Grid.Column="0" Width="80" Height="20" >
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding AccId}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <ComboBox Name="accRecCombo" Grid.Row="1" Grid.Column="1" Width="80" Height="20" >
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding AccId}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Button Name="selectSenButton" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="20" Click="selectSenButton_Click">Select an account</Button>
        <Button Name="selectRecButton" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="20" Click="selectRecButton_Click">Select an account</Button>
        <TextBlock Name="senderAcc" Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center">Account ID(sender):</TextBlock>
        <TextBlock Name="recieverAcc" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">Account ID(reciever):</TextBlock>
        <TextBlock Grid.Row="4" HorizontalAlignment="Center" VerticalAlignment="Center">Available Amount To Send</TextBlock>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Name="money" Grid.Row="4" Grid.Column="1">0</TextBlock>
        <TextBlock Grid.Row="5" HorizontalAlignment="Center" VerticalAlignment="Center">Amount</TextBlock>
        <TextBox Grid.Row="5" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Name="amountBox" Width="180"></TextBox>
        <Button Name="ConfrimTranButton" Grid.Row="7" Grid.ColumnSpan="2" Width="150" Height="20" Click="ConfrimTranButton_Click" >Confirm Transaction</Button>
        
        
    </Grid>
</Window>
